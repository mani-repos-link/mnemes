{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "mkdir -p ../../dist && go build -o ../../dist/api ./cmd/server",
    "test": "go test ./..."
  }
}
